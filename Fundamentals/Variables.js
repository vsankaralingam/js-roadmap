// varibales are containers for storing data values
// JavaScript uses the keyword 'var', 'let', or 'const' to declare variables

let message;

// Assign a value to the variable

message = "Hey there!";

// Display the value of the variable in the Console
// we can use console.log() or alert() to display the value

console.log(message);

// Display the value of the variable in an alert box

alert(message);

// Variables can be declared using 'var', 'let', or 'const'

let firstName = "sankara",
lastName = "lingam",
age = 25;

// var can be used to declare variables, but it is less preferred in modern JavaScript

var city ="vellore";
var food = "biryani";

// const is used to declare constants, which cannot be reassigned

const myBirthday = "March 5, 20001";
 
// uppercase constants

const THEME_COLORS = {
    RED: "#FF0000",
    GREEN: "#00FF00",
    BLUE: "#0000FF"
    ORANGE: "#FFA500"
};

let currentTheme = THEME_COLORS.RED;
console.log("currentTheme);

// declare two variables

let admin, name;
name = "sankara";
admin = name;
alert(admin); // "sankara"



